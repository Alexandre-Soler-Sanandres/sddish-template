# CATALOG.md

Project-specific catalog data. This file's counterpart, `agent-harness/CATALOG.md`, defines the universal
structure and location conventions; this file tracks this project's live state within that structure.

## Active Implementation Plans

`IMPLEMENTATION-PLAN.md`'s Parallel-Work rules (`IPL-05-*`) require this table to reflect all Plans at status `ready` or `in-progress`.
Update it in the same pass a Plan's status changes into or out of either value. `ready` records a Plan that has
passed its own readiness checks and received explicit user approval — it is the status Implementing consumes.

| Plan ID | Spec ID | Status | Notes |
| --- | --- | --- | --- |

Currently empty — no Plan is `ready` or `in-progress`.

Future per-project live-state categories, if any emerge, get their own section here rather than a new file.
