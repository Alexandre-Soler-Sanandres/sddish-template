# CATALOG.md

Project-specific catalog data. This file's counterpart, `agent-harness/CATALOG.md`, defines the universal
structure and location conventions; this file tracks this project's live state within that structure.

## Active Implementation Plans

`CORE.md`'s Parallel Work rule requires this table to reflect all Plans at status `approved` or `in-progress`.
Update it in the same pass a Plan's status changes into or out of either value.

| Plan ID | Spec ID | Status | Notes |
| --- | --- | --- | --- |

Currently empty — no Plan is `approved` or `in-progress`.

Future per-project live-state categories, if any emerge, get their own section here rather than a new file.
